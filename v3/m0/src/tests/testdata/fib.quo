fn fib(n: Int64): Int64 {
	if (n == 1 || n == 2) {
		return 1;
	}
	return fib(n - 1) + fib(n - 2);
}

fn Main() {
  let i: Int64 = 1;
	while (i <= 10) {
		Print(fib(i));
		Print(" ");
		i = i + 1;
	}
}
